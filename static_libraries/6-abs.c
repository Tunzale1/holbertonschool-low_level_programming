#include "main.h"

/**
 *_abs -  used for check letter, lowercase or uppercase
 * @n: is char
 * Return: absolute valeu
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
